# import pandas as pd
import numpy as np

from decision_tree_implementation import *

# NOTE: Algorithm works with datasets as Numpy arrays!
def BruteForceAlgorithm(dataset):
    aa = np.ndarray##
    number_of_attributes = dataset.shape[1]
    number_of_objects = dataset.shape[0]
    avaiable_attributes = range(number_of_attributes - 1)

    



